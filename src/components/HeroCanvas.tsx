// src/components/HeroCanvas.tsx
// Placeholder — full 3D implementation added in Task 9

interface HeroCanvasProps {
  scrollProgress: number
}

export default function HeroCanvas(_props: HeroCanvasProps) {
  return <canvas data-testid="hero-canvas-real" />
}
